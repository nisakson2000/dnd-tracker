// Single source of truth for the app version.
// APP_VERSION  — the overall Codex application release.
// DM_MODE_VERSION — the DM campaign engine (built separately, versioned independently).
// Bump these when releasing updates.
export const APP_VERSION = 'V0.5.2';
export const DM_MODE_VERSION = 'V0.2.0 BETA';
